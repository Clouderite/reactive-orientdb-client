language: scala

sudo: required
dist: trusty

scala:
- 2.11.8

notifications:
  email:
  - dembol@devesion.com

branches:
  only:
  - master

jdk:
- oraclejdk7
- oraclejdk8

matrix:
  include:
  - scala: 2.12.0
    jdk: oraclejdk8

addons:
  apt:
    packages:
      - oracle-java8-installer

script: ./build/run_tests.sh

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot